refactor classes:
  move stuff into configs (yaml)
    (done) python3 conversion
    (done) scenario setup
    (done) need method to take list of countries and test posture
    (done) get scenario selection menu to derive from scenarios.yml
    (done) map setup
  refactor cards
    * tie setup events to removal of cards
    * needs a dsl instead of per card logic

  extract board elements into Board class
      * state change methods
      (done) need to convert 'board tracking' variables in scenario setup to 'board tracking' dependent methods.
      (done) boad summary
      (done) country summary
      * reserves

  extract game rules and enforcement into GameEngine
    * stick mechanics from card in board class

  extract jihadist ai into jihadist bot class
    * add in ai difficulty level

  UICmd
    (done) status
    (done) deploy
    (done) withdraw
    (done) regime change
    (done) prettify scenario setup stdout
    * reassessment
    * war of ideas
    * alert
    * disrupt
    * turn
    * jihadist card plays
    * colorize output
    * jihad actions
* fix saving

